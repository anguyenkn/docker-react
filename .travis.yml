language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t anguyenkn/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true anguyenkn/docker-react npm run test
